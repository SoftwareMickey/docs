---
title: "beaver plan"
description: "Preview a deployment without changing production state."
---

## Examples

```bash
beaver plan [--env <environment>] [--json]
```

Resolves the current project and selected environment, then previews the deployment. Use `--env` for an environment override and `--json` for automation.
