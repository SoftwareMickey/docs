---
title: "beaver init"
description: "Prepare the current directory for Beaver."
---

## Examples

```bash
beaver init
beaver init --guided
beaver init --propose
```

Creates or synchronizes this directory's Beaver configuration. Use `--guided` for a narrated setup or `--propose` to preview changes without writing them. Continue with [`beaver validate`](/cli/validate).
