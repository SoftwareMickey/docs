---
title: "beaver status"
description: "Compare the declared project configuration with live state."
---

## Examples

```bash
beaver status [--env <environment>] [--explain]
```

Shows configuration drift for the current project. Use `--env` to select an environment and `--explain` to include a suggested next command.
