---
title: "beaver validate"
description: "Validate project configuration without deploying."
---

## Examples

```bash
beaver validate
```

Checks the current directory's Beaver configuration and reports the first issue to fix. It never changes a deployment. Run it before [`beaver plan`](/cli/plan) or [`beaver deploy`](/cli/deploy).
