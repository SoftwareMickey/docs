---
title: "beaver version"
description: "Print the installed Beaver CLI version."
---

## Examples

<Info>
  **Reviewed against source**: examples on this page were checked against `beaver-cli` source and its own `--help` output, not executed end-to-end against a live workspace.
</Info>

```bash
beaver version
beaver --version
beaver -v
```

Include this output when reporting a CLI issue.
