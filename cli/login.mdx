---
title: "beaver login"
description: "Authenticate a Beaver account."
---

## Examples

```bash
beaver login
beaver login --password
BEAVER_TOKEN=<token> beaver login
```

The default flow opens browser authentication. Use `--password` for email/password or `BEAVER_TOKEN` in a headless environment.
