---
title: "beaver shell"
description: "Open the optional interactive Beaver shell."
---

## Examples

```bash
beaver shell
```

The default `beaver` command deploys the current project. Use `beaver shell` when you prefer a persistent interactive session with command history.
