---
title: "beaver agent"
description: "Manage the local Beaver background service."
---

## Examples

```bash
beaver agent install
beaver agent status
beaver agent logs
```

Install and inspect the service that keeps an enrolled machine available to Beaver. Use `beaver agent start` or `beaver agent stop` to control it. Run `beaver doctor` when setup or connectivity fails.
